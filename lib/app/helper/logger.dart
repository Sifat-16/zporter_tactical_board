import 'package:logger/logger.dart';

void zlog({required dynamic data, Level? level}) {
  // if (kDebugMode) {
  //   Logger logger = sl.get<Logger>();
  //   logger.log(Level.info, data);
  // } else {}
}
